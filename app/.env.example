# Self Protocol Configuration
# IMPORTANT: Contract addresses must be lowercase to match on-chain scope calculation

# Sender contract address (deployed on Celo Sepolia)
NEXT_PUBLIC_SELF_ENDPOINT=0xyour_sender_contract_address

# Receiver contract address (deployed on Base Sepolia)
NEXT_PUBLIC_RECEIVER_ADDRESS=0xyour_receiver_contract_address

# Application name shown in QR code
NEXT_PUBLIC_SELF_APP_NAME="Self + Hyperlane Workshop"

# Scope seed used to derive the contract's scope
# Must match the SCOPE_SEED used during contract deployment
NEXT_PUBLIC_SELF_SCOPE_SEED="proof-of-human-hyperlane"
